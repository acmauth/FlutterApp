import 'package:flutter/widgets.dart';

class BlankPadding extends Padding {
  const BlankPadding({Key key})
      : super(
          key: key,
          padding: const EdgeInsets.all(10),
        );
}
